void setup() {

Serial.begin(9600);
pinMode(A0, INPUT); 
}
void loop() {

int val = analogRead(A0);

Serial.print("value is "); 
Serial.println(val);  // analog read value stored in integer

 // Serial print string on monitor

delay(100); // the value of val is printed on serial
}
